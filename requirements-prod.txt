# Production requirements — excludes heavy ML packages (chromadb, sentence-transformers)
# to stay within Render free tier 512MB RAM limit.
# RAG features are available in local development with full requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
PyGithub>=2.5.0
python-dotenv>=1.0.0
mcp>=1.0.0
langgraph>=0.2.0
langchain-core>=0.3.0
anthropic>=0.40.0
groq>=0.9.0
httpx>=0.27.0
pydantic>=2.9.0
